/** Declaration file generated by dts-gen */

export = Apollo;

declare class Apollo {
    constructor(appInfo: {
      configServerUrl: string;
      appId: string | number;
      cluster?: string;
      listenOnNotification?: boolean;
      initialConfigs: { [key: string]: object };
    });

    public fetchConfig({ key, namespace }: { key: string, namespace?: string }): any;

    public fetchConfigs({ keys, namespace }: { keys: string[], namespace?: string }): any;

    public refreshConfigs({ configs, namespace }: { configs: object, namespace: string }): boolean;

    private errorHandler(errorMessage: any): void;

    private fetchConfigsFromCache(namespace: any): any;

    private fetchConfigsFromDB(namespace: any): any;

    private fetchKnownNamespace(): any;

    private initializeWithConfigs(currentConfigs: any): void;

    private startListenOnNotification(retryTimes: any): any;
}
